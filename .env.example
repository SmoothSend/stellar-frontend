# Stellar Frontend Environment
# Copy to .env and set values. For Vercel, add these in Project Settings → Environment Variables.

# Option A: SDK via proxy (recommended - add localhost:5173 to CORS in dashboard)
VITE_SMOOTHSEND_API_KEY=pk_nogas_your_key_here

# Option B: Direct relayer (fallback when no API key)
VITE_RELAYER_URL=http://localhost:3001
